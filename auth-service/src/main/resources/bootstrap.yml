server:
  port: 8083

spring:
  application:
    name: auth-service
  cloud:
    nacos:
      server-addr: ${NACOS_URL}
      discovery:
        namespace: dextea
      config:
        namespace: dextea
        file-extension: yaml