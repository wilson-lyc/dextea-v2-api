server:
  port: 8083

spring:
  application:
    name: auth-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_URL}
        namespace: dextea
        group: admin
      config:
        server-addr: ${NACOS_URL}
        namespace: dextea
        group: admin
        prefix: ${spring.application.name}
        file-extension: yaml