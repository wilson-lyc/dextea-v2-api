
spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: ${NACOS_URL}
      discovery:
        namespace: dextea
      config:
        namespace: dextea
        file-extension: yaml