server:
  port: 8084

spring:
  application:
    name: tos-service
  cloud:
    nacos:
      server-addr: ${NACOS_URL}
      discovery:
        namespace: dextea
      config:
        namespace: dextea
        file-extension: yaml