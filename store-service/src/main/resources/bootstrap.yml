server:
  port: 8082

spring:
  application:
    name: store-service
  cloud:
    nacos:
      server-addr: ${NACOS_URL}
      discovery:
        namespace: dextea
      config:
        namespace: dextea
        file-extension: yaml
