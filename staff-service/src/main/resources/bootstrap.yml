server:
  port: 8081

spring:
  application:
    name: staff-service
  cloud:
    nacos:
      server-addr: ${NACOS_URL}
      discovery:
        namespace: dextea
      config:
        namespace: dextea
        file-extension: yaml